<table id="productos">
    <tr>
        <th>ID</th>
        <th>Código</th>
        <th>Nombre</th>
        <th>Stock</th>
        <th>Precio</th>
        <th>Borrar</th>
    </tr>
    {{#each products}}
        <tr>
            <td>{{this._id}}</td>
            <td>{{this.code}}</td>
            <td>{{this.title}}</td>
            <td>{{this.stock}}</td>
            <td>{{this.price}}</td>
            <td><button class="deleteButton" id="deleteButton_{{this._id}}" onclick="deleteProduct({{this._id}})">Borrar</button></td>
        </tr>
    {{/each}}
</table>

<script src="/socket.io/socket.io.js"></script>
<script src="/js/products.js"></script>